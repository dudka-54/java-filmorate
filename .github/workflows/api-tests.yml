name: Films API Tests

on:
  pull_request:

jobs:
  build:
    uses: yandex-praktikum/java-filmorate/.github/workflows/api-tests.yml@ci
    with:
      # Добавьте параметр для пропуска Postman тестов (если поддерживается)
      skip-postman: true